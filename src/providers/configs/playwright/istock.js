export default {
  name: 'IStock', // Matches provider name in the class
  // searchUrl: '', // Not needed if using Getty API directly via the class
  // selectors: {}, // Not needed for API mode
  // imageExtraction: {}, // Not needed for API mode
  // fullSizeActions: {}, // Not needed for API mode
  // playwrightOptions: {}, // Not needed for API mode
  requiresApiKey: true, // Uses the same API key as Getty Images
  apiKeyInstructions: "Uses the same API key as Getty Images. Get your key from Getty Images developer portal."
};
